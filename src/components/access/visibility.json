{
  "collectionName": "components_access_content_visibilities",
  "info": {
    "displayName": "Visibility",
    "icon": "eye",
    "description": "Controls content publishing status, scheduling, and expiration."
  },
  "options": {},
  "attributes": {
    "contentStatus": {
      "type": "enumeration",
      "enum": [
        "Unpublished",
        "Published",
        "Scheduled"
      ],
      "default": "Unpublished",
      "required": true
    },
    "scheduledDate": {
      "type": "datetime",
      "description": "The date and time when content should be published.",
      "required": false
    },
    "expirationDate": {
      "type": "datetime",
      "description": "The date and time when content should expire.",
      "required": false
    }
  }
}